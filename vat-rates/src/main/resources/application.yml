spring:
    application:
        name: vat-rates-service
    config:
        import: optional:configserver:http://config-server:8888/config
#        import: optional:configserver:http://root:1234@localhost:8888/config

#need this url prop here because of tests
eu:
    vatrates:
        url: https://euvatrates.com/rates.json
